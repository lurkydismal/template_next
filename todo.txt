Fix file upload

Improve how file link looks in table

Enable upload for filed when dragging to field in form

Do not open row dialog when clicking "Open image" in table

Show file upload progress

Show what file was selected

Fix image preview not closing on Escape key

Add checkbox and other boolean field types
Add Toggle button field type
Add Slider field type
Add map field type

Implement SpeedDial with context based on if row dialog is open or not

Implement "composite" field type to contain multiple fields
Implement option "stackable" to auto-add new field when its filled to
support any amount of this field and send it as array in form data
Organize all react-hook-form usage into @components/rhf
Implement settings
Implement auto-adding settings for fields aka changing the corner of markdown
toggle for some field
Consider NextAuth
Save diff for all text field changes and store it in some format that takes
less size
Restore SSE connection on crash

Use uppy for file handling
Implement infering field config from db schema

Implement dashboard with graphs, administration, messaging as sending
notifications
